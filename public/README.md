# Public assets

- Place resume PDFs in `public/resumes/` (for example `public/resumes/resume.pdf`).
- Resume metadata (name/title/label/uri) lives in `src/data/resumes.json`.
- Add `profile.jpg` (or update `profileImageUrl` in `src/data/about.json`) for your sidebar profile photo.
